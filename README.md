# ABSCalCut (public deploy)

Built web app only — safe to publish as a **public** repository.

- No service-role or OpenAI keys here
- Supabase anon key is embedded in JS (expected for client apps)

Deploy via GitHub Pages or any static host.
